# aurora-agent 0.4.0

Adds the first-class AuroraSeal AI Output v3 client.

- `AIOutputClient`
- DIGEST_ONLY, REDACTED, and FULL_PAYLOAD helpers
- create/read/seal/verify API operations
- atomic Verification Bundle download
- exact JSON validation with float rejection
- structured API and transport errors

The existing local action-evidence and compositional-ingestion surfaces remain
compatible.