AURORA-AGENT PUBLIC DISTRIBUTION APPROVAL RECORD
================================================

This record documents approval for the public distribution of the specified
aurora-agent release. It does not extend the license scope to AURORA hosted
services or proprietary JAKROW components.

Approval date: 2026-08-17
Package: aurora-agent
Approved version: 0.9.0
Approved release tag: v0.9.0
License: Apache License, Version 2.0 (SPDX: Apache-2.0)

Approved scope:
- the aurora-agent Python package source under src/aurora_agent/;
- package metadata and public documentation required for distribution;
- examples and independent verification challenges included in the source
  distribution;
- Python wheel and source archives generated from this public repository.

Explicitly excluded from this approval and license scope:
- the AURORA backend, frontend, hosted verification, infrastructure, and
  services;
- JAKROW repository content and components not included in this public
  aurora-agent repository;
- AURORA and JAKROW trademarks, names, logos, and branding.

Release conditions:
1. The release tag must be v0.9.0 and must match the version declared in
   pyproject.toml.
2. The complete test suite must pass on supported Python versions.
3. Wheel and source distributions must pass build and metadata validation.
4. TestPyPI and production PyPI Trusted Publishers must match their respective
   GitHub Actions workflows and protected environments (`testpypi` and `pypi`).
5. Release artifacts must include SHA-256 checksums, a CycloneDX SBOM, and
   GitHub-native build provenance attestations.
6. Production publication must run from the approved v0.9.0 tag and the
   production publish workflow must be explicitly confirmed at dispatch time.
7. The public quickstart must be validated using a clean installation from the
   public package index after publication.

Approval statement:
"The aurora-agent SDK source and its generated distribution artifacts are
approved for public distribution under the Apache License 2.0. The AURORA
backend, frontend, hosted services, infrastructure, and JAKROW components
outside this public repository are explicitly excluded from the scope of this
license."